<div class="container-fluid">
	<div class="row">
		<div class="col-md-offset-2 col-md-8">
			<div class="row">

				<div class="col-md-5">
					<h3>Membros</h3>
						<table class="table">
							<thead>
								<td>Nome</td>
								<td>Email</td>
							</thead>
							<tbody>
								<% @members.find_each do |member| %>
									<tr>
										<td><%= member.name %></td>
										<td><%= member.email %></td>
										<td><%= link_to destroy_member_path(member), method: :delete, data: {confirm: "Tem certeza de que deseja excluir este membro?"} do %>
													<i class="glyphicon glyphicon-trash"></i>
												<% end %>
										</td>
									</tr>
								<% end %>
							</tbody>
						</table>
					<%= button_tag "Novo membro", class: "btn btn-primary", remote: true, data: {toggle: "modal", target: "#member-register-modal"} %>
				</div>

				<div class="col-md-offset-2 col-md-5">
					<h3>Enquetes</h3>

					<% @polls.find_each do |poll| %>
						<div class="pull-left">
							<h1 class="rating-num"><%= poll.final_result %></h1>
			        <div class="rating">
			          <span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star">
			          </span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star">
			          </span><span class="glyphicon glyphicon-star-empty"></span>
			        </div>
			      </div>
		        <div class="pull-right text-right">
			        <div>
			        	<span><%= poll.created_at.strftime("%d/%m/%Y as %H:%M") %></span>
			        </div>
		        	<span class="glyphicon glyphicon-user"></span> &nbsp;
		        	<%= poll.voting_histories.count %> votaram
		        </div>
		        <div class="clearfix"></div>
		        <hr />
					<% end %>

					<!-- <table class="table">
						<thead>
							<td>Data</td>
							<td>Nota parcial</td>
							<td>Resultado final</td>
						</thead>
						<tbody>
							<% @polls.find_each do |poll| %>
								<tr>
									<td><%= poll.created_at.strftime("%d/%m/%Y as %H:%M") %></td>
									<td><%= poll.partial_result %></td>
									<td><%= poll.final_result %></td>
								</tr>
							<% end %>
						</tbody>
					</table> -->
					<%= form_tag ('/polls') do %>
						<%= submit_tag "Criar enquete", class: "btn btn-primary", data: {confirm: "Tem certeza de que deseja criar? Isso enviará o link de votação para todos os membros.", disable_with: "Convidando membros. Por favor, aguarde..."} %>
					<% end %>
				</div>

			</div>
		</div>
	</div>
</div>

<%= render 'modal_form' %>