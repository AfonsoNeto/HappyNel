<%= form_for(@poll) do |f| %>
  <% if @poll.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@poll.errors.count, "error") %> prohibited this poll from being saved:</h2>

      <ul>
      <% @poll.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :acumulated_score %><br>
    <%= f.text_field :acumulated_score %>
  </div>
  <div class="field">
    <%= f.label :final_result %><br>
    <%= f.text_field :final_result %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
